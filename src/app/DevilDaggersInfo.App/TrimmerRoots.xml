<linker>
	<!-- All API models must be preserved, otherwise trimming will remove property setters which breaks JSON deserialization. -->
	<assembly fullname="DevilDaggersInfo.Web.ApiSpec.App" preserve="all" />
</linker>
